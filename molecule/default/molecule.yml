---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: ansible-role-node-exporter-${MOLECULE_IMAGE:-rsprta/alpine-ansible}
    image: ${MOLECULE_IMAGE:-rsprta/alpine-ansible}
    command: ''
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    pre_build_image: true
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
